# Copy to .env.local for local development.
# Both are required to enable the Face Progress Community feature.
# Without them, Community runs in local demo mode (posts stay on this device only).
# See README.md "Face Progress Community setup" for how to get these values.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
